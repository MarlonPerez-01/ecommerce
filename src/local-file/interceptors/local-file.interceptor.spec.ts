import { LocalFileInterceptor } from './local-file.interceptor';

describe('LocalFileInterceptor', () => {
  it('should be defined', () => {
    expect(new LocalFileInterceptor()).toBeDefined();
  });
});
